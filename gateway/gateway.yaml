apiVersion: gateway.networking.k8s.io/v1
kind: Gateway 
metadata:
  name: k-gateway
  namespace: gateway-system
spec:
  gatewayClassName: kgateway
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces:
          from: All